================================================================================
📚 PROJECT SUMMARY: gRPC-POC
================================================================================
สรุป: POC สำหรับ Microservices Architecture + gRPC + Observability Stack
ปรับปรุง: December 2025

================================================================================
🏗️ PROJECT ARCHITECTURE OVERVIEW
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENT REQUEST                                 │
│                          (HTTP: POST /api/v1/auth/register)                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              KONG API GATEWAY                               │
│                              (port: 8000)                                   │
│                                                                             │
│   - รับ HTTP Request แปลงเป็น gRPC ผ่าน grpc-gateway plugin                    │
│   - Route ไปยัง Orchestrator Service                                         |
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                               gRPC (port: 50052)
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          ORCHESTRATOR SERVICE                               │
│                              (port: 50052)                                  │
│                                                                             │
│   หน้าที่:                                                                     │
│   - รับ request จาก API Gateway                                              │
│   - Coordinate การทำงานระหว่าง services                                      |
│   - เรียก Domain Service ผ่าน gRPC                                            │
│                                                                             │
│   APIs:                                                                     │
│   - Register (POST /api/v1/auth/register)                                   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                               gRPC (port: 50051)
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            DOMAIN SERVICE                                   │
│                              (port: 50051)                                  │
│                                                                             │
│   หน้าที่:                                                                     │
│   - จัดการ Business Logic                                                    |
│   - ติดต่อกับ Database (PostgreSQL)                                            |
│   - CRUD operations สำหรับ User                                              |
│                                                                             │
│   APIs:                                                                     │
│   - CreateUser                                                              │
│   - GetUserList                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                              TCP (port: 5432)
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              POSTGRESQL                                     │
│                              (port: 5432)                                   │
│                                                                             │
│   Tables:                                                                   │
│   - users (id, first_name, last_name, email, password)                      |
│   - user_histories (audit log)                                              |
└─────────────────────────────────────────────────────────────────────────────┘


================================================================================
📊 OBSERVABILITY STACK
================================================================================

All services ส่ง telemetry data ไปยัง OpenTelemetry Collector:

┌─────────────────────────────────────────────────────────────────────────────┐
│                         APPLICATION SERVICES                                │
│                   (Domain + Orchestrator)                                   │
│                                                                             │
│   ส่ง Telemetry Data:                                                        │
│   ├── Traces (distributed tracing)                                          |
│   ├── Logs (application logs)                                               |
│   └── Metrics (request count, latency, etc.)                                |
└─────────────────────────────────────────────────────────────────────────────┘
                               │
                        OTLP gRPC (4317)
                               ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                      OTEL-COLLECTOR (4317, 4318, 8889)                      │
│                                                                             │
│   - รับ OTLP data จาก applications                                           |
│   - Batch processing เพื่อประสิทธิภาพ                                           |  
│   - กระจาย data ไปยัง backends ตาม pipeline                                  |
└─────────────────────────────────────────────────────────────────────────────┘
           │                    │                    │
           ▼                    ▼                    ▼
    ┌───────────┐        ┌───────────┐        ┌───────────┐
    │   TEMPO   │        │   LOKI    │        │PROMETHEUS │
    │  (3200)   │        │  (3100)   │        │  (9090)   │
    │           │        │           │        │           │
    │  Traces   │        │   Logs    │        │  Metrics  │
    │ 30 days   │        │ 30 days   │        │time-series│
    └─────┬─────┘        └─────┬─────┘        └─────┬─────┘
          │                    │                    │
          └────────────────────┼────────────────────┘
                               ▼
                    ┌─────────────────┐
                    │    GRAFANA      │
                    │    (3000)       │
                    │                 │
                    │  Dashboard &    │
                    │  Visualization  │
                    └─────────────────┘


================================================================================
📁 PROJECT STRUCTURE
================================================================================

grpc-poc/
├── cmd/                        # Application entry points
│   ├── domain/main.go          # Domain service entry
│   └── orchestrator/main.go    # Orchestrator service entry
│
├── internal/                   # Internal packages (non-exportable)
│   ├── domain/                 # Domain service
│   │   ├── config.go           # Configuration struct
│   │   ├── initial/            # Dependency injection & initialization
│   │   │   ├── initial.go      # App struct & lifecycle
│   │   │   ├── client.go       # External clients (DB, OTEL)
│   │   │   ├── repository.go   # Repository initialization
│   │   │   ├── sdk.go          # SDK initialization (validator)
│   │   │   └── use_case.go     # UseCase initialization
│   │   ├── pb/                 # Protocol Buffers
│   │   │   ├── domain_service.proto
│   │   │   └── gen/            # Generated gRPC code
│   │   ├── repository/         # Data access layer
│   │   │   ├── user.go
│   │   │   └── user_history.go
│   │   └── usecase/            # Business logic
│   │       └── user/
│   │           ├── create/     # CreateUser usecase
│   │           └── getlist/    # GetUserList usecase
│   │
│   └── orchestrator/           # Orchestrator service
│       ├── config.go           # Configuration struct
│       ├── initial/            # Dependency injection
│       ├── pb/                 # Protocol Buffers + google/api for grpc-gateway
│       └── usecase/
│           └── auth/
│               └── register/   # Register usecase
│
├── pkg/                        # Shared packages (exportable)
│   ├── db/postgres/            # PostgreSQL connection & base repository
│   ├── grpcx/                  # gRPC client utilities
│   ├── otel/                   # OpenTelemetry setup
│   └── validatorx/             # Validation utilities
│
├── deployment/docker/          # Docker configurations
│   ├── golang/Dockerfile       # Multi-stage build for Go services
│   ├── kong/kong.yaml          # Kong API Gateway config
│   ├── otel/                   # OpenTelemetry Collector config
│   ├── tempo/                  # Tempo (traces) config
│   ├── loki/                   # Loki (logs) config
│   ├── grafana/                # Grafana datasources config
│   └── prometheus/             # Prometheus config
│
├── docker-compose.yaml         # All services orchestration
├── Makefile                    # Build & run commands
├── domain.env                  # Domain service environment
└── orchestrator.env            # Orchestrator service environment


================================================================================
🔧 TECHNOLOGY STACK
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│ LAYER           │ TECHNOLOGY              │ PURPOSE                         │
├─────────────────┼─────────────────────────┼─────────────────────────────────┤
│ Language        │ Go (Golang)             │ High performance, gRPC support  │
│ Framework       │ gRPC + Protocol Buffers │ Service-to-service communication│
│ API Gateway     │ Kong + grpc-gateway     │ HTTP to gRPC transcoding        │
│ Database        │ PostgreSQL 18           │ Relational data storage         │
│ ORM             │ GORM (inferred)         │ Database operations             │
│ Validation      │ go-playground/validator │ Request validation              │
│ Configuration   │ caarlos0/env + godotenv │ Environment management          │
│ Tracing         │ OpenTelemetry + Tempo   │ Distributed tracing             │
│ Logging         │ OpenTelemetry + Loki    │ Centralized logging             │
│ Metrics         │ OpenTelemetry + Prometheus │ Application metrics          │
│ Visualization   │ Grafana                 │ Dashboards & exploration        │
│ Containerization│ Docker + Docker Compose │ Service deployment              │
└─────────────────┴─────────────────────────┴─────────────────────────────────┘


================================================================================
📡 SERVICE PORTS SUMMARY
================================================================================

┌──────────────────────┬────────┬─────────────────────────────────────────────┐
│ SERVICE              │ PORT   │ DESCRIPTION                                 │
├──────────────────────┼────────┼─────────────────────────────────────────────┤
│ Kong (HTTP Proxy)    │ 8000   │ Public HTTP API endpoint                    │
│ Kong (HTTPS Proxy)   │ 8443   │ Public HTTPS API endpoint                   │
│ Kong (Admin API)     │ 8001   │ Kong admin interface                        │
│ Orchestrator         │ 50052  │ gRPC service                                │
│ Domain               │ 50051  │ gRPC service                                │
│ PostgreSQL           │ 5432   │ Database                                    │
│ OTEL Collector gRPC  │ 4317   │ OTLP gRPC receiver                          │
│ OTEL Collector HTTP  │ 4318   │ OTLP HTTP receiver                          │
│ OTEL Prometheus      │ 8889   │ Metrics endpoint for Prometheus             │
│ Tempo                │ 3200   │ Trace query API                             │
│ Loki                 │ 3100   │ Log query API                               │
│ Prometheus           │ 9090   │ Metrics query & Web UI                      │
│ Grafana              │ 3000   │ Visualization Web UI                        │
└──────────────────────┴────────┴─────────────────────────────────────────────┘


================================================================================
🚀 QUICK START COMMANDS
================================================================================

# Start all infrastructure services
docker-compose up -d

# Run domain service locally
make rundomain

# Run orchestrator service locally
make runorchestrator

# Generate protobuf code
make proto                    # Generate all proto files
make domainproto              # Generate domain service proto
make orchestratorproto        # Generate orchestrator service proto

# Test API
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "first_name": "John",
    "last_name": "Doe",
    "password": "password123",
    "confirm_password": "password123"
  }'


================================================================================
🔍 ACCESS POINTS
================================================================================

- API Endpoint:  http://localhost:8000
- Grafana:       http://localhost:3000 (no login required)
- Prometheus:    http://localhost:9090
- Loki:          http://localhost:3100
- Tempo:         http://localhost:3200


================================================================================
📋 API ENDPOINTS
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│ ORCHESTRATOR SERVICE (HTTP via Kong)                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│ POST /api/v1/auth/register                                                 │
│   Request:                                                                  │
│   {                                                                         │
│     "email": "string",                                                      │
│     "first_name": "string",                                                 │
│     "last_name": "string",                                                  │
│     "password": "string",                                                   │
│     "confirm_password": "string"                                            │
│   }                                                                         │
│   Response:                                                                 │
│   {                                                                         │
│     "token": "string"                                                       │
│   }                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ DOMAIN SERVICE (gRPC only - internal)                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│ rpc CreateUser(CreateUserRequest) returns (CreateUserResponse)             │
│ rpc GetUserList(GetUserListRequest) returns (GetUserListResponse)          │
└─────────────────────────────────────────────────────────────────────────────┘


================================================================================
🎯 KEY DESIGN PATTERNS
================================================================================

1. CLEAN ARCHITECTURE
   - UseCase Layer: Business logic ที่ไม่ขึ้นกับ framework
   - Repository Layer: Data access abstraction
   - Handler Layer: gRPC handler implementation

2. DEPENDENCY INJECTION
   - initial/ package: จัดการ dependency ทั้งหมด
   - Constructor injection pattern
   - Graceful shutdown handling

3. SERVICE MESH PATTERN
   - Kong เป็น API Gateway
   - gRPC สำหรับ service-to-service communication
   - Centralized observability

4. DISTRIBUTED TRACING
   - Trace context propagation ผ่าน gRPC metadata
   - Correlation ระหว่าง logs และ traces ด้วย trace_id


================================================================================
📊 GRAFANA QUERY EXAMPLES
================================================================================

# ดู Logs ตาม service name (Loki)
{service_name="domain-service"}

# ค้นหา logs ด้วย trace_id (Loki)
{service_name=~".+"} | json | trace_id="<your-trace-id>"

# ดู request count (Prometheus)
rate(http_requests_total[5m])


================================================================================
✨ SUMMARY
================================================================================

Project นี้เป็น POC สำหรับ:
- Microservices Architecture ด้วย gRPC
- API Gateway (Kong) สำหรับ HTTP-to-gRPC transcoding
- Full Observability Stack (Traces, Logs, Metrics)
- Clean Architecture pattern ใน Go
- Docker-based deployment

เหมาะสำหรับเป็น reference สำหรับ:
- การออกแบบ Microservices
- การใช้งาน OpenTelemetry ใน Go
- การ setup Observability stack (Grafana + Tempo + Loki + Prometheus)
- การใช้ Kong เป็น API Gateway กับ gRPC

================================================================================